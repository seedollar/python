# An example of how to use the dict() built in Python function. The dict() method essentially
# creates a new dictionary object from provided parameters.

new_dictionary = dict({'name': 'Oliver', 'age': 60})
print(type(new_dictionary))
print(new_dictionary)
